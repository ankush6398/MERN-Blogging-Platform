// src/utils/toast.js
import { toast as toastify } from 'react-toastify';

// Provide a default export so existing `import toast from ...` works
const toast = toastify;

export default toast;
export { toast };
